<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/login"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/search_bg_shadow" >
   <!--      <com.fourmob.panningview.PanningView
            android:id="@+id/panningView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/ic_launcher"
             /> -->
    <RelativeLayout
        android:id="@+id/facebook_id"
        android:layout_width="250dp"
        android:layout_height="52dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="28dp"
        android:background="@drawable/round_corner" >

        <LinearLayout
            android:id="@+id/shadow_id"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_margin="1dp"
            android:background="@drawable/drop_shadow" >

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:layout_marginBottom="2dp"
                android:background="@drawable/round_corner_two"
                android:gravity="center"
                android:text="@string/fb_butn_text"
                android:textColor="@color/white" >
            </TextView>
        </LinearLayout>
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/gpuls__layout_id"
        android:layout_width="250dp"
        android:layout_height="52dp"
        android:layout_below="@+id/facebook_id"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="28dp"
        android:background="@drawable/round_corner" >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_margin="1dp"
            android:layout_marginBottom="48dp"
            android:background="@drawable/drop_shadow" >

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:layout_marginBottom="2dp"
                android:background="@drawable/round_corner_gpuls"
                android:gravity="center"
                android:text="@string/google_butn_text"
                android:textColor="@color/white" >
            </TextView>
        </LinearLayout>
    </RelativeLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:layout_below="@+id/gpuls__layout_id" />

</RelativeLayout>